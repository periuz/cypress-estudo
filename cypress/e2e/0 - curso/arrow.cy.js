it ('nada agora', function(){ })

//function soma(a, b) {
//    return a + b
//} 


//Sempre que vamos declarar uma função em JS usamos CONST agora. Se for variável, usamos LET. 
//const soma = function (a, b) {
//    return a + b
//}

// Arrow function é uma função que não tem o this, ou seja, não tem contexto.
//const soma = (a, b) => {
//    return a + b
//}

//const soma = (a, b) => a + b // Arrow function de uma linha só.

//const soma = a => a + a //Quando tem apenas um argumento, nao precisa de ()

